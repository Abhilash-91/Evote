<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>New question</title>
</head>
<body class="bg-yellow-900">
    <%- include('header.ejs') %>
    <div class="flex gap-1 mx-10 mt-6 text-white">
        <span>></span>
        <a href="/elections" class="text-black-400 hover:text-white transition-all">All Elections</a>
        <span>></span>
        <a href="/elections/<%= id %>/" class="text-black-400 hover:text-white transition-all">Election</a>
        <span>></span>
        <a href="/elections/<%= id %>/questions" class="text-black-400 hover:text-white transition-all">Questions</a>
    </div>
    <div class=" flex text-black-800 antialiased flex-col text-white overflow-hidden ">
        <div class=" py-3 sm:w-96 mx-auto text-center">
            <span class="text-black-200">Add a new Question</span>
            <div>
                <% if (messages && messages.error && messages.error.length > 0) { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="bg-black-600 text-white my-3 list-none rounded-md px-4 py-2"><%= messages.error[i] %></p>
                    <% } %>
                <% } %>
            </div>
            <div class="text-black-200">
                <div class="h-2 bg-white rounded-t-md"></div>
                <form action="/elections/<%= id %>/questions/create" method="post" class="px-8 py-6">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                    <label for="question" class="text-black-200">What is the question?</label>
                    <input id="question" name="question" type="text" placeholder="Question" class="text-black border w-full h-5 px-3 py-5 mt-2 hover:outline-none focus:outline-none focus:ring-black-200 focus:ring-1 rounded-md" required>
                    <label for="description" class="block mt-3 font-semibold">Description</label>
                    <textarea id="description" name="description" class="text-black border w-full px-2 py-1 mt-2 hover:outline-none focus:outline-none focus:ring-black-200 focus:ring-1 rounded-md" placeholder="Description" rows="4" cols="20"></textarea>
                    <button type="submit" class="text-black-200">Add question</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>